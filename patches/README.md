# Patches

Place `.patch` files here to apply to the dotnet/runtime source before building.

Patches are applied in alphabetical order by `scripts/apply-patches.ps1`.
If no `.patch` files are present, the build uses the unmodified runtime source.
